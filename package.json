{
  "name": "@metabindai/metabind-web",
  "version": "0.0.0",
  "private": true,
  "description": "Metabind web SDKs: agent client + Vercel AI SDK adapter, plus an example app",
  "type": "module",
  "packageManager": "pnpm@9.15.9",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "clean": "pnpm -r exec rm -rf dist"
  },
  "devDependencies": {
    "typescript": "~5.6.2"
  },
  "pnpm": {
    "overrides": {
      "@assistant-ui/store": "0.2.11",
      "@assistant-ui/tap": "0.5.11"
    }
  }
}
